description = "Instant session start — loads context silently, ready immediately"
prompt = """
1. Read ./CONTEXT.md silently. If it's missing or just placeholders, say "Run /setup first." and stop.
2. Scan ./context/ silently.
3. Say: "Ready. What are we doing?"
"""
