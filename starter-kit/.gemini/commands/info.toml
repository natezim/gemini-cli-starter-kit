description = "Show available commands, features, and how to use this workspace"
prompt = """
Show the user what's available in this workspace:

"Here's what you can do:

Commands:
  /start — start or resume a session (captures user, snapshots files)
  /setup — rebuild project context from scratch
  /session:save — end session: versioning, cleanup review, chat log, handoff
  /version <file> — manually snapshot a file as v1/v2/v3
  /context:update — add something permanent to project context
  /memory add <fact> — persist a fact across sessions
  /memory show — see persisted facts
  /restore — undo any file change
  /info — show this message

Workspace:
  context/ — reference files, READ-ONLY, auto-loaded every session
  output/queries/ — your SQL query library
  output/code/ — scripts, code
  output/reports/ — analyses, docs
  output/data/ — exports (CSVs, JSON)
  output/audit-log.md — structured audit trail (every write/exec/query)
  output/prompts/ — your chat prompts saved at session end
  .gemini/skills/ — skills that activate automatically
  PREFERENCES.md — customize behavior (tone, workflow overrides)
  CONTEXT.md — your project context, built by /start

Skills (copy from the repo's skills/ folder into .gemini/skills/ — only installed skills are active):
  excel/ — Formulas, dynamic arrays, Power Query, Power Pivot/DAX, VBA, Office Scripts
  python/ — Data analysis, ML pipelines, pandas/NumPy, visualization, testing
  powerbi/ — Power BI reports, DAX, semantic models, TMDL, XMLA, Fabric
  bigquery/ — BigQuery optimization, partitioning, cost control, bq CLI
  tableau-server/ — Tableau Server admin, PostgreSQL repository, RMT, jobs, auditing
  tableau-optimizer/ — Tableau performance, TWBX parsing, Power BI migration
  tableau-bigquery/ — Tableau + BigQuery live connections, SQL push-down"

How it works:
  I test everything before handing it back. I log every task and query automatically.
  I ask before doing anything risky. I never touch production without explicit approval.
  If I'm unsure about anything, I'll ask."
"""
