# Project Context

Auto-loaded every session. Written by you (or by Gemini during `/start` first-time setup).

To pick up changes mid-session after editing this file, run `/memory refresh` (built-in).

<!--
Sections this file typically has:

# Project Context
One-paragraph project description.

## Conventions
- Naming, layout, processes specific to this project.

## Schemas / systems
- Key tables, APIs, files Gemini should know about.

Project-specific reference files (DDL, dbt manifests, docs) go in ./context/
instead of being pasted here. ./context/ is auto-loaded and READ-ONLY.

For DURABLE LEARNED facts the agent has discovered (recurring quirks, prefs,
gotchas), see ./MEMORY.md — that file is agent-managed.
-->
